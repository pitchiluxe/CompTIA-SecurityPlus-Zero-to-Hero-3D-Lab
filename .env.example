# Presence/WebSocket server for live user counter
# Frontend reads VITE_ prefixed variables at build time.
VITE_PRESENCE_WS=ws://localhost:4000

# Server-side CORS origin (optional). Leave empty to allow any origin.
PRESENCE_ORIGIN=
